# Stashly Backend

Express 5 + MongoDB API. See [main README](../README.md) for full setup and env vars.

```bash
npm install
cp .env.example .env
npm run dev     # build + start on :3000
npm run build
npm start
```
